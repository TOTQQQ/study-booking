spring:
  datasource:
    url: jdbc:mysql://localhost:3306/study_room_miniprogram?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456

  redis:
    host: localhost
    port: 6379

# 开启调试模式
debug: true

logging:
  level:
    com.studyroom: debug
    org.springframework.web: debug

# 开发环境JWT过期时间（短一些，方便测试）
jwt:
  access-token-expire: 3600000   # 1小时
  refresh-token-expire: 86400000 # 1天

wechat:
  miniapp:
    mock-login: true
